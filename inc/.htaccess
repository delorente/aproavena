# Nada de inc/ se sirve por HTTP: solo se incluye desde PHP.
# Vale tanto para Apache 2.4 como para 2.2.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
